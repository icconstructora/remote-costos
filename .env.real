# Cargado automáticamente por "npm run dev:real" (vite --mode real).
# Modo real: login con tu cuenta Microsoft (cuenta IC) y llamadas reales a
# https://api.icconstructora.co usando tu propio token.
VITE_AUTH_MODE=real
